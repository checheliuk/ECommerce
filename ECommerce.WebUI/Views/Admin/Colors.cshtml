@model IEnumerable<ECommerce.Domain.Model.Color>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<br />
<p>
    @Html.ActionLink(Resource.Add, "editcolor")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.ActionLink(Resource.Edit, "editcolor", new { arg1 = item.ColorID })
            </td>
        </tr>
    }
</table>

